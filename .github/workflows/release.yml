name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag to (re-)release, e.g. v3.24.0 — leave empty to use current package.json version'
        required: false
        type: string

jobs:
  release:
    name: Build & publish GitHub Release + npm
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag_name && format('refs/tags/{0}', inputs.tag_name) || github.ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          registry-url: 'https://registry.npmjs.org'

      - name: Resolve version from package.json
        id: pkg
        run: echo "version=v$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Check if GitHub Release already exists
        id: gh_check
        run: |
          TAG="${{ inputs.tag_name || steps.pkg.outputs.version }}"
          if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" &>/dev/null 2>&1; then
            echo "skip=true"  >> $GITHUB_OUTPUT
            echo "GitHub Release $TAG already exists — skipping release creation."
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Check if npm version already published
        id: npm_check
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if npm view "@thebuoyant-tsdev/mui-ts-library@${VERSION}" version &>/dev/null 2>&1; then
            echo "skip=true"  >> $GITHUB_OUTPUT
            echo "npm version ${VERSION} already published — skipping npm publish."
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Install dependencies
        if: steps.gh_check.outputs.skip == 'false' || steps.npm_check.outputs.skip == 'false'
        run: npm ci

      - name: Test → Build → Pack
        if: steps.gh_check.outputs.skip == 'false' || steps.npm_check.outputs.skip == 'false'
        run: npm run pack-release

      - name: Publish GitHub Release
        if: steps.gh_check.outputs.skip == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag_name || steps.pkg.outputs.version }}
          name: ${{ inputs.tag_name || steps.pkg.outputs.version }}
          files: releases/*.tgz
          generate_release_notes: true

      - name: Publish to npm
        if: steps.npm_check.outputs.skip == 'false'
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
